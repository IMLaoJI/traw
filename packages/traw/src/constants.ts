import { TRCamera } from 'types';

export const SLIDE_WIDTH = 1600;
export const SLIDE_HEIGHT = 900;
export const SLIDE_RATIO = SLIDE_WIDTH / SLIDE_HEIGHT;

export const DEFAULT_CAMERA: TRCamera = {
  center: { x: 0, y: 0 },
  zoom: 1,
};
